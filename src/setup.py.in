from distutils.core import setup
import os

setup(
    name='pygpb',
    version='${PACKAGE_VERSION}',
    author='Laurent Lejeune',
    author_email='laurent.lejeune@artorg.unibe.ch',
    description='global probability boundary',
    packages=['pygpb'],
    package_dir={'pygpb': '${CMAKE_CURRENT_BINARY_DIR}/pygpb'},
    package_data = {'pygpb': ['libpygpb.so']}
)
